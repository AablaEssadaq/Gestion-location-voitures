<Window x:Class="LocationVoiture.Admin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gestion Location - Accueil" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen" Background="#F0F2F5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- === LIGNE 0 : BARRE D'OUTILS === -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,20">

            <!-- Bouton Employés -->
            <Button Content="👥 Gérer les Employés" Click="BtnUsers_Click"
                    Height="40" Width="180" Margin="0,0,15,0"
                    Background="White" Foreground="#4B5563" FontWeight="Bold" BorderBrush="#E5E7EB" BorderThickness="1" Cursor="Hand"/>

            <!-- Bouton Voitures -->
            <Button Content="+ AJOUTER UNE VOITURE" Click="BtnAjouter_Click"
                    Height="40" Width="200"
                    Background="#2563EB" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>

        <!-- === LIGNE 1 : LISTE VOITURES === -->
        <DataGrid Grid.Row="1" x:Name="MyDataGrid" 
                  AutoGenerateColumns="True" CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column"
                  Background="White" BorderBrush="#E5E7EB" BorderThickness="1">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Padding" Value="10"/>
                    <Setter Property="Background" Value="#F9FAFB"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="#E5E7EB"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
        </DataGrid>
    </Grid>
</Window>