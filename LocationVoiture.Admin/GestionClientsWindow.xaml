<Window x:Class="LocationVoiture.Admin.GestionClientsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gestion des Clients" Height="680" Width="1150"
        WindowStartupLocation="CenterScreen" Background="#F3F4F6">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="👥 Fichier Clients" FontSize="24" FontWeight="Bold" Foreground="#1F2937"/>
                <TextBlock Text=" | Base de données" FontSize="16" Foreground="#6B7280" VerticalAlignment="Bottom" Margin="10,0,0,3"/>
            </StackPanel>

            <Button HorizontalAlignment="Right" Content="+ NOUVEAU CLIENT" Click="BtnAjouter_Click"
                    Height="40" Padding="20,0" Background="#2563EB" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="10" Margin="0,0,0,15" BorderBrush="#E5E7EB" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">
                    <TextBox x:Name="txtRecherche" Height="35" Padding="30,5,5,5" VerticalContentAlignment="Center"
                     TextChanged="TxtRecherche_TextChanged" Background="#F9FAFB" BorderBrush="#D1D5DB"/>
                    <TextBlock Text="🔍 Nom, Prénom, Email, Permis..." IsHitTestVisible="False" 
                       Foreground="#9CA3AF" VerticalAlignment="Center" Margin="10,0,0,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=txtRecherche}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>

                <ComboBox x:Name="cbTri" Grid.Column="2" Height="35" SelectionChanged="CbTri_SelectionChanged" Background="#F9FAFB">
                    <ComboBoxItem Content="Derniers inscrits" Tag="Id DESC" IsSelected="True"/>
                    <ComboBoxItem Content="Nom (A-Z)" Tag="Nom ASC"/>
                    <ComboBoxItem Content="Nom (Z-A)" Tag="Nom DESC"/>
                </ComboBox>

                <Button Grid.Column="4" Content="📥 Exporter" Click="BtnExporter_Click" Width="90" Height="35" 
                Background="#059669" Foreground="White" FontWeight="Bold" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button Grid.Column="6" Content="📤 Importer" Click="BtnImporter_Click" Width="90" Height="35" 
                Background="#2563EB" Foreground="White" FontWeight="Bold" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button Grid.Column="8" Content="↺ Reset" Click="BtnReset_Click" 
                Padding="10,0" Height="35" Background="White" BorderBrush="#D1D5DB" BorderThickness="1" Foreground="#4B5563">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Border>
        <DataGrid Grid.Row="2" x:Name="ClientsGrid" AutoGenerateColumns="False" 
                  CanUserAddRows="False" IsReadOnly="True"
                  Background="White" BorderBrush="#E5E7EB" BorderThickness="1" RowHeight="40">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nom" Binding="{Binding Nom}" Width="*"/>
                <DataGridTextColumn Header="Prénom" Binding="{Binding Prenom}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="1.5*"/>
                <DataGridTextColumn Header="Téléphone" Binding="{Binding Telephone}" Width="120"/>
                <DataGridTextColumn Header="N° Permis" Binding="{Binding NumPermis}" Width="120"/>
                <DataGridTemplateColumn Header="Actions" Width="130">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="✏️" Click="BtnModifier_Click" Background="#DBEAFE" Foreground="#1E40AF" BorderThickness="0" Padding="8,4" Margin="0,0,5,0" Tag="{Binding Id}"/>
                                <Button Content="🗑️" Click="BtnSupprimer_Click" Background="#FEE2E2" Foreground="#DC2626" BorderThickness="0" Padding="8,4" Tag="{Binding Id}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="3" Background="White" CornerRadius="0,0,8,8" Padding="10" BorderBrush="#E5E7EB" BorderThickness="1,0,1,1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock x:Name="lblPaginationInfo" Text="Page 1 sur 1" VerticalAlignment="Center" Margin="0,0,20,0" Foreground="Gray"/>
                <Button x:Name="btnPrev" Content="◀ Précédent" Click="BtnPrev_Click" Width="100" Height="30" Margin="0,0,10,0" Background="White" BorderBrush="#D1D5DB"/>
                <Button x:Name="btnNext" Content="Suivant ▶" Click="BtnNext_Click" Width="100" Height="30" Background="White" BorderBrush="#D1D5DB"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>