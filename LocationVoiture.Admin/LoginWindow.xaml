<Window x:Class="LocationVoiture.Admin.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Connexion Administrateur" Height="550" Width="900"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">

    <Border CornerRadius="20" Background="White">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="1.5*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="40">
                <StackPanel VerticalAlignment="Center">

                    <TextBlock Text="Bienvenue" 
                               FontSize="32" FontWeight="Bold" Foreground="#333" 
                               Margin="0,0,0,5"/>
                    <TextBlock Text="Connectez-vous au Back-Office" 
                               FontSize="16" Foreground="#777" 
                               Margin="0,0,0,40"/>

                    <TextBlock Text="IDENTIFIANT" FontSize="12" FontWeight="Bold" Foreground="#999" Margin="5,0,0,5"/>
                    <Border BorderThickness="1" BorderBrush="#E0E0E0" CornerRadius="8" Background="#F9F9F9" Margin="0,0,0,20">
                        <TextBox x:Name="txtUsername" Height="45" VerticalContentAlignment="Center" 
                                 BorderThickness="0" Background="Transparent" Padding="10,0" FontSize="14"/>
                    </Border>

                    <TextBlock Text="MOT DE PASSE" FontSize="12" FontWeight="Bold" Foreground="#999" Margin="5,0,0,5"/>
                    <Border BorderThickness="1" BorderBrush="#E0E0E0" CornerRadius="8" Background="#F9F9F9" Margin="0,0,0,30">
                        <PasswordBox x:Name="txtPassword" Height="45" VerticalContentAlignment="Center" 
                                     BorderThickness="0" Background="Transparent" Padding="10,0" FontSize="14"/>
                    </Border>

                    <Button x:Name="btnLogin" Content="SE CONNECTER" Click="BtnLogin_Click"
                            Height="50" Foreground="White" FontSize="14" FontWeight="Bold" Cursor="Hand"
                            BorderThickness="0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#2563EB"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="10">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1D4ED8"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="Quitter l'application" Margin="0,20,0,0"
                            Background="Transparent" BorderThickness="0" Foreground="#999" Cursor="Hand"
                            Click="BtnQuitter_Click" HorizontalAlignment="Center"/>

                    <TextBlock x:Name="lblError" Text="Identifiants incorrects" Foreground="#EF4444" 
                               HorizontalAlignment="Center" Margin="0,15,0,0" Visibility="Collapsed"/>
                </StackPanel>
            </Grid>

            <Border Grid.Column="1" CornerRadius="0,20,20,0" ClipToBounds="True">
                <Grid>
                    <Image Source="https://images.unsplash.com/photo-1492144534655-ae79c964c9d7?q=80&amp;w=1000&amp;auto=format&amp;fit=crop" 
                           Stretch="UniformToFill"/>

                    <Border Background="#2563EB" Opacity="0.2"/>

                    <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="20"
                            Background="Transparent" BorderThickness="0" Foreground="White" FontSize="18" FontWeight="Bold"
                            Cursor="Hand" Click="BtnQuitter_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}">
                                                <ContentPresenter/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground" Value="#FFaaaa"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>